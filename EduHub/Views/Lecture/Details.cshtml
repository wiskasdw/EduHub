@model EduHub.Models.Lecture

@{
    ViewData["Title"] = "Детали лекции";
}

<h1>@ViewData["Title"]</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Название:</dt>
        <dd class="col-sm-10">@Model.Title</dd>

        <dt class="col-sm-2">Описание:</dt>
        <dd class="col-sm-10">@Model.Description</dd>

        <dt class="col-sm-2">Файл:</dt>
        <dd class="col-sm-10">
            <a href="~/uploads/@Model.FileName" target="_blank">Скачать PDF</a>
        </dd>

        <dt class="col-sm-2">Дата загрузки</dt>
        <dd class="col-sm-10">@Model.UploadDate</dd>
    </dl>
</div>
<div>
    <a asp-action="Index">Назад к списку</a>
</div>